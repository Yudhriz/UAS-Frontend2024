// Variablr URL API: Base URL APIconst
const URL = "https://covid-fe-2023.vercel.app/api/";

// Variable Endpoint (object)

const endpoint = {
  global: `${URL}global.json`,
  indonesia: `${URL}indonesia.json`,
};

export default endpoint;
