import styled from "styled-components";

const StyledAlert = styled.p`
  .form__error {
    color: red;
    text-align: left;
    font-size: 0.8rem;
    margin-top: 5px;
  }
`;

export default StyledAlert;
